defmodule Excov do
  def plus(x, y, flag // true) do
    if flag do
      x + y
      x + y
    else
      x + y
      x + y
    end
  end

  def minus(x, y) do
    x - y
  end

  def multiply(x, y) do
    x * y
  end
end
